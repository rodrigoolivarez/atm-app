//  export const handleKeyPress = (number: string) => {
        
//      if (monto.length < 10) {
//          setMonto((prevMonto) => prevMonto + number);
//      }
//  };

// export const handleClear = () => {
//      setMonto('');
//  };

//  export const handleDelete = () =>{
//      setMonto((prev) => prev.slice(0, -1))
//  }

export const valentin = () => {
    console.log('holamundo')
}